var assert = require('chai').assert;
var noPermsDigits = require('../challenges/combinatorial-exploration');

describe('Combinatorial Exploration', function() {

  it('return number of digits of number of possible permutations for n unique items', function() {
    assert.equal(noPermsDigits(0), 1);
    assert.equal(noPermsDigits(1), 1);
    assert.equal(noPermsDigits(2), 1);
    assert.equal(noPermsDigits(3), 1);
    assert.equal(noPermsDigits(4), 2);
    assert.equal(noPermsDigits(5), 3);
    assert.equal(noPermsDigits(6), 3);
    assert.equal(noPermsDigits(7), 4);
    assert.equal(noPermsDigits(8), 5);
    assert.equal(noPermsDigits(9), 6);
    assert.equal(noPermsDigits(10), 7);
    assert.equal(noPermsDigits(11), 8);
    assert.equal(noPermsDigits(12), 9);
    assert.equal(noPermsDigits(13), 10);
    assert.equal(noPermsDigits(14), 11);
    assert.equal(noPermsDigits(15), 13);
    assert.equal(noPermsDigits(16), 14);
    assert.equal(noPermsDigits(17), 15);
    assert.equal(noPermsDigits(18), 16);
    assert.equal(noPermsDigits(19), 18);
    assert.equal(noPermsDigits(25), 26);
    assert.equal(noPermsDigits(50), 65);
    assert.equal(noPermsDigits(75), 110);
    assert.equal(noPermsDigits(100), 158);
    assert.equal(noPermsDigits(125), 210);
    assert.equal(noPermsDigits(150), 263);
    assert.equal(noPermsDigits(175), 319);
    assert.equal(noPermsDigits(200), 375);
    assert.equal(noPermsDigits(225), 434);
    assert.equal(noPermsDigits(250), 493);
    assert.equal(noPermsDigits(275), 554);
    assert.equal(noPermsDigits(300), 615);
    assert.equal(noPermsDigits(325), 677);
    assert.equal(noPermsDigits(350), 741);
    assert.equal(noPermsDigits(375), 805);
    assert.equal(noPermsDigits(400), 869);
    assert.equal(noPermsDigits(425), 935);
    assert.equal(noPermsDigits(450), 1001);
    assert.equal(noPermsDigits(475), 1067);
    assert.equal(noPermsDigits(500), 1135);
    assert.equal(noPermsDigits(525), 1202);
    assert.equal(noPermsDigits(550), 1271);
    assert.equal(noPermsDigits(575), 1339);
    assert.equal(noPermsDigits(600), 1409);
    assert.equal(noPermsDigits(625), 1478);
    assert.equal(noPermsDigits(650), 1548);
    assert.equal(noPermsDigits(675), 1619);
    assert.equal(noPermsDigits(700), 1690);
    assert.equal(noPermsDigits(725), 1761);
    assert.equal(noPermsDigits(750), 1833);
    assert.equal(noPermsDigits(775), 1905);
    assert.equal(noPermsDigits(800), 1977);
    assert.equal(noPermsDigits(825), 2050);
    assert.equal(noPermsDigits(850), 2123);
    assert.equal(noPermsDigits(875), 2197);
    assert.equal(noPermsDigits(900), 2270);
    assert.equal(noPermsDigits(925), 2344);
    assert.equal(noPermsDigits(950), 2419);
    assert.equal(noPermsDigits(975), 2493);
  });
});